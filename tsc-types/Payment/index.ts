export enum PaymentMethods {
    paypal = "paypal",
    stripe = "stripe",
    default = "",
  }
  
  export enum PaymentRouteStatus {
    PENDING = "pending",
    SUCCESS = "success",
    FAIL = "fail",
  }